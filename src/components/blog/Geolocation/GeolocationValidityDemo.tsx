/**
 * TODO: Implement per spec in content/blog/geolocation-element.mdx:Demo 2
 * Gotchas playground — isValid / invalidReason with live style knobs.
 * Spec: see refined demo specs (validity demo) in conversation.
 */
export default function GeolocationValidityDemo() {
  return (
    <div className="my-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-neutral-700 dark:bg-neutral-800">
      <p className="text-sm text-slate-700 dark:text-slate-300">
        GeolocationValidityDemo placeholder — implementation pending.
      </p>
      <p className="mt-1 text-xs text-slate-500 dark:text-slate-400">
        Will expose isValid / invalidReason with contrast, font-size, opacity,
        hidden/covered controls.
      </p>
    </div>
  )
}
